<UserControl x:Class="eConcierge.Resort.Presentation.EventCalendarCategoryToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:eConcierge.Resort.Presentation.Commands"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <c:CommandReference x:Key="AddCommandReference" Command="{Binding AddCommand}" />
    </UserControl.Resources>
    <StackPanel HorizontalAlignment="Left">
        <Button Content="Add" Width="60" HorizontalAlignment="Left" Margin="0,0,0,5" Command="{Binding AddCommand}" />
        <DataGrid ItemsSource="{Binding Categories}" x:Name="grd" Width="200" AutoGenerateColumns="False" DataGridRow.MouseDoubleClick="DataGrid_MouseDoubleClick"   >
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Name}" Header="Name"
                                     Width="180">                    
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
